version: prometheus/v1
service: ser1
labels:
  product_id: erfef
  env: stage
  org_id: TSE
slos:
  - name: rfrf
    objective: 90
    description: rfrf
    sli:
      events:
        error_query: rfrf1
        total_query: rfrf
    alerting:
      name: rfrf
      labels:
        category: Latency
      annotations:
        summary: rfrf
      page_alert:
        labels:
          severity: critical
      ticket_alert:
        labels:
          severity: warning
